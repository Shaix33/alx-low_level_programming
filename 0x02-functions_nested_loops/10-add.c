#include "main.h"
/**
 * add - adds two integers and returns the result
 * description - a function that adds 2 inegers and returns the result
 * @a: first number to add
 * @b: second number to add
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int res = a + b;

	return (res);
}
